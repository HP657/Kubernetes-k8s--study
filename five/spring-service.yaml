apiVersion: v1
kind: Service

metadata:
  name: spring-service

spec:
  type: NodePort
  selector:
    app: backend-app
  ports:
    - protocol: TCP
      port: 8080 # 쿠버네티스 내부 서비스에 접속하기 위한 포트
      targetPort: 8080 # 매핑하기 위한 파드 포트
      nodePort: 30000 # 외부 접속 포트